#include "netlist.hpp"

#include "suraj_parser.h"

NetlistMap* surajParserLoadNetlist(void) {
    NetlistMap *netlist = new NetlistMap();

    // Assuming suraj parser has been invoked


    return netlist;
}
